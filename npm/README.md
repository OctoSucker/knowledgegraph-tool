# knowledgegraph-tool npm wrapper

This package installs the prebuilt `kgtool` binary from GitHub Releases.

Install globally:

```bash
npm install -g knowledgegraph-tool
```

After install:

```bash
kgtool --help
```

The installer downloads the release that matches the current OS and CPU.
